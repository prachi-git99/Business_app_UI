import 'package:flutter/material.dart';
class HashScreen extends StatelessWidget {
  const HashScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.purple,);
  }
}